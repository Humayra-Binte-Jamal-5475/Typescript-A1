TypeScript Interview Questions — Explained in Bangla
1️⃣ Interface এবং Type এর মধ্যে পার্থক্য

TypeScript-এ interface এবং type—দুটোই ডেটার স্ট্রাকচার নির্ধারণ করতে ব্যবহৃত হয়। তবে তাদের মধ্যে কিছু গুরুত্বপূর্ণ পার্থক্য আছে।

✔ Interface

মূলত object structure নির্ধারণের জন্য ব্যবহৃত হয়

একই interface কে একাধিকবার declare করলে তারা merge হয়ে যায়

extend করা খুবই সহজ

উদাহরণ
interface User {
  name: string;
}

interface User {
  age: number;
}

// Final merged interface → { name: string; age: number }

✔ Type

primitive, union, tuple, intersection—সব ধরনের টাইপ define করা যায়

type alias re-open বা merge করা যায় না

বেশি flexible এবং versatile

উদাহরণ
type ID = number | string;
type Person = { name: string; age: number };

✔ তুলনা (সংক্ষেপে)
বিষয়	Interface	Type
Merge হয়	✅ হ্যাঁ	❌ না
Object define	✅ হ্যাঁ	✅ হ্যাঁ
Union/Intersection	❌ সীমিত	✅ সম্পূর্ণ
Extend	সহজ	করা যায়

👉 Conclusion: Interface সাধারণত object shape-এর জন্য, আর type বেশি flexible এবং powerful।

2️⃣ any, unknown এবং never এর মধ্যে পার্থক্য

এগুলো TypeScript-এর খুবই গুরুত্বপূর্ণ তিনটি বিশেষ টাইপ।

✔ any

সবচেয়ে flexible কিন্তু সবচেয়ে risky

যে কোনো টাইপ assign করা যায়

type checking প্রায় বন্ধ করে দেয়

let x: any = 5;
x = "hello";
x = true;

✔ unknown

any-এর মতোই সব ধরনের ভ্যালু গ্রহণ করতে পারে

কিন্তু ব্যবহার করার আগে টাইপ চেক করা বাধ্যতামূলক (safe)

let x: unknown = 10;

if (typeof x === "number") {
  console.log(x + 5);
}


👉 unknown = “I accept anything but you must check me before using.”

✔ never

যেসব ফাংশন কখনো কোনো ভ্যালু return করে না, তাদের টাইপ হয় never।

উদাহরণ
function throwError(): never {
  throw new Error("Error occurred!");
}

function infiniteLoop(): never {
  while (true) {}
}

✔ সংক্ষেপে
any	সবকিছু গ্রহণ করে, unsafe
unknown	সব গ্রহণ করে কিন্তু ব্যবহার করতে হলে check করতে হয়
never	কখনো return করে না এমন ফাংশনের টাইপ
